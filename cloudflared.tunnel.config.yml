# This is for unbuffered responses when running under a Cloudflare Tunnel.
# The server should default to streamed responses with the tunnel buffering it (removing streaming)
# Unless this configuration file includes this line:
noChunkedEncoding: false
